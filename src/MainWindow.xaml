<Window x:Class="MarkdownViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MarkdownViewer"
        xmlns:vm="clr-namespace:MarkdownViewer.ViewModels"
        xmlns:models="clr-namespace:MarkdownViewer.Models"
        xmlns:conv="clr-namespace:MarkdownViewer.Converters"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="{Binding WindowTitle}"
        Height="700" Width="1100"
        MinHeight="500" MinWidth="800"
        Background="{DynamicResource WindowBg}"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" Invert="False"/>
        <conv:BoolToVisibilityConverter x:Key="InverseBoolToVis" Invert="True"/>
        <conv:InverseBoolConverter x:Key="InverseBool"/>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Command="{Binding OpenFileCommand}" Key="O" Modifiers="Ctrl"/>
        <KeyBinding Command="{Binding ToggleTocCommand}" Key="T" Modifiers="Ctrl+Shift"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Toolbar -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBg}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="8,6">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="📂 Open" Command="{Binding OpenFileCommand}"
                            Background="{DynamicResource ButtonBg}"
                            Foreground="{DynamicResource TextPrimary}"
                            BorderThickness="0" Padding="10,5" Cursor="Hand"
                            FontSize="12">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"
                               Margin="4,0"/>

                    <Button x:Name="RecentButton" Click="RecentButton_Click"
                            Background="{DynamicResource ButtonBg}"
                            Foreground="{DynamicResource TextPrimary}"
                            BorderThickness="0" Padding="8,5" Cursor="Hand"
                            FontSize="12">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🕐 " Foreground="{DynamicResource TextDimmed}"/>
                            <TextBlock Text="Recent ▾" Foreground="{DynamicResource TextPrimary}"/>
                        </StackPanel>
                    </Button>

                    <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"
                               Margin="4,0"/>

                    <Button Command="{Binding ToggleTocCommand}"
                            Background="{DynamicResource ButtonBg}"
                            Foreground="{DynamicResource TextPrimary}"
                            BorderThickness="0" Padding="10,5" Cursor="Hand"
                            FontSize="12">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="☰ TOC"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <Button Command="{Binding ToggleThemeCommand}"
                            Background="{DynamicResource AccentBrush}"
                            BorderThickness="0"
                            Padding="8,4" Cursor="Hand" FontSize="13"
                            ToolTip="{Binding ThemeTooltip}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                        <TextBlock Text="{Binding ThemeIcon}"
                                   Foreground="White"/>
                    </Button>
                    <TextBlock Text="{Binding StatusText}"
                               Foreground="{DynamicResource TextDimmed}" FontSize="11"
                               VerticalAlignment="Center" Margin="4,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Recent Files Popup -->
        <Popup x:Name="RecentPopup" PlacementTarget="{Binding ElementName=RecentButton}"
               Placement="Bottom" StaysOpen="False" PopupAnimation="Fade"
               Width="340" AllowsTransparency="True">
            <Border Background="{DynamicResource ElevatedBg}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                    CornerRadius="6" Padding="4">
                <StackPanel>
                    <TextBlock Text="RECENT FILES" Foreground="{DynamicResource TextDimmed}"
                               FontSize="10" Margin="8,4" FontWeight="SemiBold"/>
                    <ItemsControl ItemsSource="{Binding RecentFiles}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="models:RecentFileEntry">
                                <Border Padding="8,6" Margin="2,0" CornerRadius="4"
                                        Background="Transparent"
                                        MouseLeftButtonDown="RecentFile_Click"
                                        Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="📄" Margin="0,0,8,0"
                                                   Foreground="{DynamicResource TextDimmed}" FontSize="12"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding FileName}"
                                                       Foreground="{DynamicResource TextPrimary}"
                                                       FontSize="12"/>
                                            <TextBlock Text="{Binding Directory}"
                                                       Foreground="{DynamicResource TextDimmed}"
                                                       FontSize="10"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2"
                                                   Text="missing" FontSize="9"
                                                   Foreground="#d16969"
                                                   Visibility="{Binding Exists,
                                                       Converter={StaticResource InverseBoolToVis}}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Separator Background="{DynamicResource BorderBrush}" Margin="4,2"/>
                    <Button Content="Clear recent files"
                            Background="Transparent"
                            Foreground="{DynamicResource TextDimmed}"
                            BorderThickness="0" HorizontalAlignment="Left"
                            FontSize="11" Padding="8,4" Cursor="Hand"
                            Command="{Binding ClearRecentFilesCommand}"/>
                </StackPanel>
            </Border>
        </Popup>

        <!-- Row 1: Tab Bar -->
        <Border Grid.Row="1" Background="{DynamicResource TabBg}"
                BorderBrush="{DynamicResource AccentBrush}"
                BorderThickness="0,0,0,2" Padding="0">
            <Grid>
                <ScrollViewer HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal" x:Name="TabPanel">
                        <ItemsControl ItemsSource="{Binding Tabs}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="models:TabItem">
                                    <Border Padding="10,6" MinWidth="120"
                                            Cursor="Hand"
                                            MouseLeftButtonDown="Tab_Click"
                                            Tag="{Binding}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{DynamicResource SurfaceBg}"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                                <Setter Property="Margin" Value="0,0,0,-2"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource ElevatedBg}"/>
                                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="6" Height="6"
                                                     Margin="0,0,6,0" VerticalAlignment="Center"
                                                     Fill="{DynamicResource DotActive}">
                                            </Ellipse>
                                            <TextBlock Text="{Binding FileName}"
                                                       Foreground="{DynamicResource TextPrimary}"
                                                       FontSize="11" MaxWidth="180"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <Button Content="✕" FontSize="10"
                                                    Foreground="{DynamicResource TextDimmed}"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="6,0,0,0"
                                                    Cursor="Hand"
                                                    Click="TabClose_Click"
                                                    Tag="{Binding}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Button Content="+" FontSize="14"
                                Foreground="{DynamicResource TextDimmed}"
                                Background="Transparent"
                                BorderThickness="0" Padding="8,6" Cursor="Hand"
                                Click="AddTab_Click"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Row 2: Main Content Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- TOC Sidebar -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBg}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0"
                    Width="200"
                    Visibility="{Binding ShowTocSidebar, Converter={StaticResource BoolToVis}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="ON THIS PAGE"
                               Foreground="{DynamicResource TocHeaderText}" FontSize="10"
                               FontWeight="SemiBold" Margin="10,8,10,6"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="TocItemsControl" ItemsSource="{Binding TocHeadings}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="models:TocHeading">
                                    <Border Margin="4,0" CornerRadius="3"
                                            Cursor="Hand"
                                            MouseLeftButtonDown="TocItem_Click"
                                            Tag="{Binding}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Padding" Value="8,0"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource TocHoverBg}"/>
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource TocActiveBg}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal"
                                                    Margin="{Binding Indent, StringFormat='{}{0},0,0,0'}">
                                            <Ellipse Width="4" Height="4" VerticalAlignment="Center">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="{DynamicResource DotNormal}"/>
                                                        <Setter Property="Width" Value="4"/>
                                                        <Setter Property="Height" Value="4"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Level}" Value="1">
                                                                <Setter Property="Width" Value="5"/>
                                                                <Setter Property="Height" Value="5"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Level}" Value="3">
                                                                <Setter Property="Width" Value="3"/>
                                                                <Setter Property="Height" Value="3"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Level}" Value="4">
                                                                <Setter Property="Width" Value="3"/>
                                                                <Setter Property="Height" Value="3"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                                <Setter Property="Fill" Value="{DynamicResource DotActive}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                            <TextBlock Text="{Binding Text}"
                                                       Margin="6,0,0,0"
                                                       FontSize="12"
                                                       Foreground="{DynamicResource TocItemText}"
                                                       TextTrimming="CharacterEllipsis"
                                                       ToolTip="{Binding Text}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Level}" Value="1">
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Level}" Value="3">
                                                                <Setter Property="FontSize" Value="11"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Level}" Value="4">
                                                                <Setter Property="FontSize" Value="11"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Content Area -->
            <Grid Grid.Column="1">
                <wv2:WebView2 x:Name="MarkdownWebView"
                              Visibility="{Binding HasTabs, Converter={StaticResource BoolToVis}}"/>

                <!-- Landing page (no tabs open) -->
                <Border Background="{DynamicResource WindowBg}"
                        Visibility="{Binding HasTabs, Converter={StaticResource InverseBoolToVis}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                Margin="0,-40,0,0">
                        <TextBlock Text="📘" FontSize="48"
                                   Foreground="{DynamicResource TocHeaderText}"
                                   TextAlignment="Center" Margin="0,0,0,12"/>
                        <TextBlock Text="Markdown Viewer"
                                   FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimary}"
                                   TextAlignment="Center" Margin="0,0,0,4"/>
                        <TextBlock Text="Open a Markdown file to get started"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextDimmed}"
                                   TextAlignment="Center" Margin="0,0,0,24"/>
                        <Button Content="📂 Open File" FontSize="13"
                                Command="{Binding OpenFileCommand}"
                                Background="{DynamicResource AccentBrush}" Foreground="White"
                                BorderThickness="0" Padding="14,8"
                                Cursor="Hand" HorizontalAlignment="Center">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <TextBlock Text="Ctrl+O" FontSize="11"
                                   Foreground="{DynamicResource TocHeaderText}"
                                   TextAlignment="Center" Margin="0,6,0,0"/>

                        <Border Margin="0,32,0,0" Width="320"
                                Visibility="{Binding RecentFiles.Count, Converter={StaticResource BoolToVis}}">
                            <StackPanel>
                                <TextBlock Text="RECENTLY OPENED"
                                           Foreground="{DynamicResource TocHeaderText}"
                                           FontSize="10" Margin="0,0,0,8"/>
                                <ItemsControl ItemsSource="{Binding RecentFiles}" MaxHeight="200">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="models:RecentFileEntry">
                                            <Border Padding="8,5" CornerRadius="4"
                                                    Cursor="Hand"
                                                    MouseLeftButtonDown="RecentFile_Click">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📄"
                                                               Foreground="{DynamicResource TocHeaderText}"
                                                               FontSize="12" Margin="0,0,8,0"/>
                                                    <TextBlock Text="{Binding FileName}"
                                                               Foreground="{DynamicResource TextBody}"
                                                               FontSize="12"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- Row 3: Status Bar -->
        <Border Grid.Row="3" Background="{DynamicResource StatusBarBg}" Padding="4,2,10,2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding StatusText}"
                           Foreground="White" FontSize="10"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <TextBlock Foreground="#ccd" FontSize="10"
                               Text="{Binding ActiveTab.EncodingName, FallbackValue='UTF-8'}"/>
                    <TextBlock Text="  |  " Foreground="#99b" FontSize="10"/>
                    <TextBlock Text="Markdown" Foreground="#ccd" FontSize="10"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
